const express = require('express'); const router = express.Router(); const jobController = require('../controllers/job.controller'); const { authMiddleware } = require('../middleware/authMiddleware'); const { requireRole } = require('../middleware/roleMiddleware'); router.get('/', jobController.listJobs); router.get('/:id', jobController.viewJob); router.post('/', authMiddleware, requireRole('recruiter'), jobController.createJob); module.exports = router;
