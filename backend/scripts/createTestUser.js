const { PrismaClient } = require('@prisma/client'); const { hashPassword } = require('../utils/hash'); const prisma = new PrismaClient(); (async ()=>{ try{ const hashed = await hashPassword('Rec@2004'); const user = await prisma.user.create({ data:{ name:'Rec', email:'rec@123', password:hashed, role:'recruiter' } }); console.log('Created recruiter', user); process.exit(0);}catch(err){console.error(err);process.exit(1);} })();
